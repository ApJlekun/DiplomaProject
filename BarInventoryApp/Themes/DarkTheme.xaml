<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Dark Theme Palette -->
    
    <!-- Primary Colors -->
    <Color x:Key="PrimaryColor">#3B82F6</Color> <!-- Tailwind Blue 500 -->
    <Color x:Key="PrimaryHoverColor">#2563EB</Color> <!-- Tailwind Blue 600 -->
    <Color x:Key="AccentColor">#3B82F6</Color>

    <!-- Structural Colors -->
    <Color x:Key="BackgroundColor">#000000</Color> 
    <Color x:Key="CardColor">#0F172A</Color> <!-- Very dark blue-gray -->
    <Color x:Key="BorderColor">#1E293B</Color> 

    <!-- Shadow Color -->
    <Color x:Key="ShadowColor">#000000</Color>

    <!-- Text Colors -->
    <Color x:Key="TextColor">#F9FAFB</Color> <!-- Tailwind Gray 50 -->
    <Color x:Key="TextSecondaryColor">#9CA3AF</Color> <!-- Tailwind Gray 400 -->

    <!-- Status Colors -->
    <Color x:Key="DangerColor">#EF4444</Color> <!-- Tailwind Red 500 -->
    <Color x:Key="DangerHoverColor">#DC2626</Color> <!-- Tailwind Red 600 -->
    <Color x:Key="SuccessColor">#10B981</Color> <!-- Tailwind Emerald 500 -->

    <!-- Component specific background colors -->
    <Color x:Key="ControlBackgroundColor">#000000</Color> 
    <Color x:Key="GridHeaderBackgroundColor">#0F172A</Color> 
    <Color x:Key="GridAltRowBackgroundColor">#0F172A</Color> 
    <Color x:Key="GridSelectedBackgroundColor">#1E40AF</Color> <!-- Tailwind Blue 800 -->

    <!-- Brushes -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{DynamicResource PrimaryColor}"/>
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="{DynamicResource PrimaryHoverColor}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{DynamicResource AccentColor}"/>
    <SolidColorBrush x:Key="BackgroundBrush" Color="{DynamicResource BackgroundColor}"/>
    <SolidColorBrush x:Key="CardBrush" Color="{DynamicResource CardColor}"/>
    <SolidColorBrush x:Key="BorderBrush" Color="{DynamicResource BorderColor}"/>
    <SolidColorBrush x:Key="TextBrush" Color="{DynamicResource TextColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{DynamicResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{DynamicResource DangerColor}"/>
    <SolidColorBrush x:Key="DangerHoverBrush" Color="{DynamicResource DangerHoverColor}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{DynamicResource SuccessColor}"/>
    <SolidColorBrush x:Key="ControlBackgroundBrush" Color="{DynamicResource ControlBackgroundColor}"/>
    <SolidColorBrush x:Key="GridHeaderBackgroundBrush" Color="{DynamicResource GridHeaderBackgroundColor}"/>
    <SolidColorBrush x:Key="GridAltRowBackgroundBrush" Color="{DynamicResource GridAltRowBackgroundColor}"/>
    <SolidColorBrush x:Key="GridSelectedBackgroundBrush" Color="{DynamicResource GridSelectedBackgroundColor}"/>
</ResourceDictionary>