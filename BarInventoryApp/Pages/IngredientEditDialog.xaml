<Window x:Class="BarInventoryApp.Pages.IngredientEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BarInventoryApp.Pages"
        mc:Ignorable="d"
        Title="Ингредиент" Height="420" Width="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📦 Ингредиент" 
                   Style="{StaticResource PageTitle}" 
                   FontSize="22"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Название" FontSize="13" FontWeight="Medium" 
                       Foreground="{DynamicResource TextBrush}" Margin="0,0,0,6"/>
            <TextBox Text="{Binding IngredientName, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource ModernTextBox}"
                     Margin="0,0,0,16"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Margin="0,0,12,0">
                    <TextBlock Text="Количество" FontSize="13" FontWeight="Medium" 
                               Foreground="{DynamicResource TextBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Ед. изм." FontSize="13" FontWeight="Medium" 
                               Foreground="{DynamicResource TextBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="Отмена" Click="Cancel_Click" 
                    Style="{StaticResource SecondaryButton}"
                    Padding="20,10" Margin="0,0,10,0"/>
            <Button Content="Сохранить" Click="Save_Click" 
                    Style="{StaticResource PrimaryButton}"
                    Padding="20,10"/>
        </StackPanel>
    </Grid>
</Window>
